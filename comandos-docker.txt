# Rodar container Postgre na versão 16.3
docker run --name librarydb -p 5432:5432 -e POSTGRES_PASSWORD=postgres -e POSTGRES_USER=postgres -e POSTGRES_DB=library postgres:17.6

# Rodar Pgadmin 4
 docker run --name pgadmin4 -p 15432:80 -e PGADMIN_DEFAULT_EMAIL=eduardol_martins@hotmail.com -e  PGADMIN_DEFAULT_PASSWORD=admin dpage/pgadmin4
 
 #Pra iniciar o container sem criar ele dnv tem que rodar esse codigo
 docker start -a librarydb
 docker start -a pgadmin4
